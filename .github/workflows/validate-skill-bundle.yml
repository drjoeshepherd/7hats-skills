name: validate-skill-bundle

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate skill bundle
        shell: pwsh
        run: |
          ./scripts/validate-skill-bundle.ps1

      - name: Smoke test templates
        shell: pwsh
        run: |
          ./scripts/smoke-test-templates.ps1

      - name: Smoke test routing and aliases
        shell: pwsh
        run: |
          ./scripts/smoke-test-routing.ps1

      - name: Smoke test intent routing
        shell: pwsh
        run: |
          ./scripts/smoke-test-intent-routing.ps1

      - name: Smoke test repo context gate
        shell: pwsh
        run: |
          ./scripts/smoke-test-repo-context.ps1

      - name: Smoke test MCP contract placeholder
        shell: pwsh
        run: |
          ./mcp/tests/smoke-contract-placeholder.ps1

      - name: Smoke test MCP schemas
        shell: pwsh
        run: |
          ./scripts/smoke-test-mcp-schemas.ps1

      - name: Smoke test MCP adapter dry run
        shell: pwsh
        run: |
          ./mcp/tests/smoke-adapter-dry-run.ps1


